swagger: '2.0'
info:
  title: 'Mechanic Shop API'
  description: 'API for managing customer accounts'
  version: '1.0.0'

host: 'https://api-application-factory-pattern.onrender.com'
schemes:
  - "https"
consumes:
  - "application/json"
produces:
  - "application/json"

securityDefinitions:
  bearerAuth:
    type: apiKey
    name: Authorization
    in: header
    description: "Customer token authentication. Format: Bearer <token>"

paths:
  /customers/login:
    post:
      tags:
        - Customers
      summary: "Customer login endpoint"
      description: "Validates customer credentials (email and password) and generates a customer-specific token"
      parameters:
        - in: "body"
          name: "body"
          description: "Customer login credentials"
          required: true
          schema:
            $ref: "#/definitions/CustomerLoginCredentials"
      responses:
        200:
          description: "Login successful"
          schema:
            $ref: "#/definitions/LoginResponse"
          examples:
            application/json:
              message: "Welcome back, John!"
              token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        401:
          description: "Invalid credentials"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /customers:
    post:
      tags:
        - Customers
      summary: "Register new customer"
      description: "Create a new customer account (rate limited to 30 per day)"
      parameters:
        - in: "body"
          name: "body"
          description: "New customer information"
          required: true
          schema:
            $ref: "#/definitions/CustomerInput"
      responses:
        201:
          description: "Customer created successfully"
          schema:
            $ref: "#/definitions/CustomerResponse"
          examples:
            application/json:
              id: 1
              first_name: "John"
              last_name: "Doe"
              email: "john@example.com"
              phone: "555-0123"
              address: "123 Main St"
        400:
          description: "Validation error"
          schema:
            $ref: "#/definitions/ErrorResponse"

    get:
      tags:
        - Customers
      summary: "Get all customers (paginated)"
      description: "Retrieve a paginated list of all customers. Results are cached for 60 seconds."
      parameters:
        - in: "query"
          name: "page"
          type: integer
          description: "Page number (default: 1)"
          required: false
        - in: "query"
          name: "per_page"
          type: integer
          description: "Items per page (default: 10)"
          required: false
      responses:
        200:
          description: "Successfully retrieved customers"
          schema:
            $ref: "#/definitions/CustomersResponse"

    put:
      tags:
        - Customers
      summary: "Update customer profile"
      description: "Update the authenticated customer's own profile information"
      security:
        - bearerAuth: []
      parameters:
        - in: "body"
          name: "body"
          description: "Updated customer information"
          required: true
          schema:
            $ref: "#/definitions/CustomerInput"
      responses:
        200:
          description: "Customer updated successfully"
          schema:
            $ref: "#/definitions/CustomerResponse"
        404:
          description: "Customer not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /customers/{customer_id}:
    get:
      tags:
        - Customers
      summary: "Get customer by ID"
      description: "Retrieve a specific customer's information by their ID"
      parameters:
        - in: "path"
          name: "customer_id"
          type: integer
          required: true
          description: "Customer ID"
      responses:
        200:
          description: "Customer found"
          schema:
            $ref: "#/definitions/CustomerResponse"
        404:
          description: "Customer not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

    delete:
      tags:
        - Customers
      summary: "Delete customer"
      description: "Delete a customer by ID (rate limited to 5 per day)"
      parameters:
        - in: "path"
          name: "customer_id"
          type: integer
          required: true
          description: "Customer ID to delete"
      responses:
        200:
          description: "Customer deleted successfully"
          schema:
            type: object
            properties:
              message:
                type: string
        404:
          description: "Customer not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /customers/profile:
    get:
      tags:
        - Customers
      summary: "Get my profile"
      description: "Retrieve the authenticated customer's profile information"
      security:
        - bearerAuth: []
      responses:
        200:
          description: "Profile retrieved successfully"
          schema:
            $ref: "#/definitions/CustomerResponse"
        404:
          description: "Customer not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /customers/my-tickets:
    get:
      tags:
        - Customers
      summary: "Get my service tickets"
      description: "Retrieve all service tickets for the authenticated customer"
      security:
        - bearerAuth: []
      responses:
        200:
          description: "Service tickets retrieved successfully"
          schema:
            type: array
            items:
              $ref: "#/definitions/ServiceTicketResponse"

  /customers/search:
    get:
      tags:
        - Customers
      summary: "Search customer by email"
      description: "Find a customer by their email address (case-insensitive search)"
      parameters:
        - in: "query"
          name: "email"
          type: string
          required: true
          description: "Customer email address to search for"
      responses:
        200:
          description: "Customer found"
          schema:
            $ref: "#/definitions/CustomerResponse"
        400:
          description: "Email parameter is required"
          schema:
            $ref: "#/definitions/ErrorResponse"
        404:
          description: "Customer not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /mechanics/login:
    post:
      tags:
        - Mechanics
      summary: "Mechanic login endpoint"
      description: "Validates mechanic credentials (email and password) and generates a mechanic-specific token"
      parameters:
        - in: "body"
          name: "body"
          description: "Mechanic login credentials"
          required: true
          schema:
            $ref: "#/definitions/MechanicLoginCredentials"
      responses:
        200:
          description: "Login successful"
          schema:
            $ref: "#/definitions/LoginResponse"
          examples:
            application/json:
              message: "Welcome back, Mike!"
              token: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        401:
          description: "Invalid credentials"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /mechanics:
    post:
      tags:
        - Mechanics
      summary: "Create new mechanic"
      description: "Register a new mechanic account in the system"
      parameters:
        - in: "body"
          name: "body"
          description: "New mechanic information"
          required: true
          schema:
            $ref: "#/definitions/MechanicInput"
      responses:
        201:
          description: "Mechanic created successfully"
          schema:
            $ref: "#/definitions/MechanicResponse"
          examples:
            application/json:
              id: 1
              first_name: "Mike"
              last_name: "Johnson"
              email: "mike@shop.com"
              specialization: "Engine Repair"
        400:
          description: "Validation error"
          schema:
            $ref: "#/definitions/ErrorResponse"

    get:
      tags:
        - Mechanics
      summary: "Get all mechanics"
      description: "Retrieve a list of all mechanics in the system"
      responses:
        200:
          description: "Successfully retrieved mechanics"
          schema:
            $ref: "#/definitions/MechanicsResponse"

    put:
      tags:
        - Mechanics
      summary: "Update mechanic profile"
      description: "Update the authenticated mechanic's own profile (rate limited to 5 per day)"
      security:
        - bearerAuth: []
      parameters:
        - in: "body"
          name: "body"
          description: "Updated mechanic information"
          required: true
          schema:
            $ref: "#/definitions/MechanicInput"
      responses:
        200:
          description: "Mechanic updated successfully"
          schema:
            $ref: "#/definitions/MechanicResponse"
        404:
          description: "Mechanic not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

    delete:
      tags:
        - Mechanics
      summary: "Delete mechanic account"
      description: "Delete the authenticated mechanic's own account"
      security:
        - bearerAuth: []
      responses:
        200:
          description: "Mechanic deleted successfully"
          schema:
            type: object
            properties:
              message:
                type: string
        404:
          description: "Mechanic not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /mechanics/profile:
    get:
      tags:
        - Mechanics
      summary: "Get my profile"
      description: "Retrieve the authenticated mechanic's profile information"
      security:
        - bearerAuth: []
      responses:
        200:
          description: "Profile retrieved successfully"
          schema:
            $ref: "#/definitions/MechanicResponse"
        404:
          description: "Mechanic not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /mechanics/my-tickets:
    get:
      tags:
        - Mechanics
      summary: "Get my assigned tickets"
      description: "Retrieve all service tickets assigned to the authenticated mechanic"
      security:
        - bearerAuth: []
      responses:
        200:
          description: "Service tickets retrieved successfully"
          schema:
            type: array
            items:
              $ref: "#/definitions/ServiceTicketResponse"
        404:
          description: "Mechanic not found"
          schema:
            $ref: "#/definitions/ErrorResponse"

  /mechanics/top-mechanics:
    get:
      tags:
        - Mechanics
      summary: "Get top mechanics by ticket count"
      description: "Retrieve the top 5 mechanics with the most assigned service tickets"
      responses:
        200:
          description: "Top mechanics retrieved successfully"
          schema:
            type: array
            items:
              $ref: "#/definitions/TopMechanicResponse"
          examples:
            application/json:
              - id: 1
                first_name: "Mike"
                last_name: "Johnson"
                email: "mike@shop.com"
                ticket_count: 47
              - id: 2
                first_name: "Sarah"
                last_name: "Williams"
                email: "sarah@shop.com"
                ticket_count: 42
        404:
          description: "No mechanics found with tickets"
          schema:
            $ref: "#/definitions/ErrorResponse"

definitions:
  CustomerLoginCredentials:
    type: object
    properties:
      email:
        type: string
        example: "john@example.com"
      password:
        type: string
        example: "password123"
    required:
      - email
      - password

  LoginResponse:
    type: object
    properties:
      message:
        type: string
      token:
        type: string

  CustomerInput:
    type: object
    properties:
      first_name:
        type: string
        example: "John"
      last_name:
        type: string
        example: "Doe"
      email:
        type: string
        example: "john@example.com"
      password:
        type: string
        example: "password123"
      phone:
        type: string
        example: "555-0123"
      address:
        type: string
        example: "123 Main St"

  CustomerResponse:
    type: object
    properties:
      id:
        type: integer
      first_name:
        type: string
      last_name:
        type: string
      email:
        type: string
      phone:
        type: string
      address:
        type: string
      password:
        type: string
        description: "Hashed password"

  CustomersResponse:
    type: array
    items:
      $ref: "#/definitions/CustomerResponse"

  ServiceTicketResponse:
    type: object
    properties:
      id:
        type: integer
      customer_id:
        type: integer
      description:
        type: string
      status:
        type: string
      created_at:
        type: string
        format: date-time

  ErrorResponse:
    type: object
    properties:
      error:
        type: string
      message:
        type: string

  MechanicLoginCredentials:
    type: object
    properties:
      email:
        type: string
        example: "mike@shop.com"
      password:
        type: string
        example: "password123"
    required:
      - email
      - password

  MechanicInput:
    type: object
    properties:
      first_name:
        type: string
        example: "Mike"
      last_name:
        type: string
        example: "Johnson"
      email:
        type: string
        example: "mike@shop.com"
      password:
        type: string
        example: "password123"
      specialization:
        type: string
        example: "Engine Repair"
      certification:
        type: string
        example: "ASE Certified"

  MechanicResponse:
    type: object
    properties:
      id:
        type: integer
      first_name:
        type: string
      last_name:
        type: string
      email:
        type: string
      password:
        type: string
        description: "Hashed password"
      specialization:
        type: string
      certification:
        type: string

  MechanicsResponse:
    type: array
    items:
      $ref: "#/definitions/MechanicResponse"

  TopMechanicResponse:
    type: object
    properties:
      id:
        type: integer
      first_name:
        type: string
      last_name:
        type: string
      email:
        type: string
      ticket_count:
        type: integer
        description: "Number of tickets assigned to this mechanic"

  ServiceTicketResponse:
    type: object
    properties:
      id:
        type: integer
      customer_id:
        type: integer
      description:
        type: string
      status:
        type: string
      created_at:
        type: string
        format: date-time

  ErrorResponse:
    type: object
    properties:
      error:
        type: string
      message:
        type: string